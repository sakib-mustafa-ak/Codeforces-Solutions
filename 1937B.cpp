
#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long t;
    cin >> t;
    while (t--)
    {
        char a[n];
        for (int i = 0; i < 2; i++)
        {
            cin >> a[i];
        }
    }
}